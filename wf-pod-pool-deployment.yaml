apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-pod-pool
  namespace: default # Change if using a different namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workflow-pod-pool
  template:
    metadata:
      labels:
        app: workflow-pod-pool
    spec:
      containers:
      - name: workflow-pod-pool
        image: nginx:latest # Replace with your Docker image
        ports:
        - containerPort: 8080
